# it actually works now
name: Approval Check Worker

on:
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  
permissions:
  contents: read
  pull-requests: write
  issues: write
  statuses: write
  
jobs:
  run-when-command:
    runs-on: ubuntu-latest

    outputs:
      number: ${{ steps.pr.outputs.number }}
      sha: ${{ steps.pr.outputs.sha }}
      is_fork: ${{ steps.pr.outputs.is_fork }}

    steps:
      - name: Get latest PR head SHA
        id: pr
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # 8.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.issue?.number ?? context.payload.pull_request?.number;
            if (!prNumber) {
              core.setFailed('Could not determine pull request number from event payload');
              return;
            }
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            core.setOutput('number', String(prNumber));
            core.setOutput('sha', pr.head.sha);
            
            // Detect if PR originates from a fork
            const baseFullName = `${context.repo.owner}/${context.repo.repo}`;
            const isFork = pr.head.repo?.full_name !== baseFullName;
            core.info(`PR #${prNumber} is_fork=${isFork}`); // debug log
            core.setOutput('is_fork', String(isFork));

  upload-meta:
    runs-on: ubuntu-latest
    needs: run-when-command
    steps:
      - name: Write PR meta to file
        run: |
          mkdir -p /tmp/pr-meta
          echo '{"pr_number": "${{ needs.run-when-command.outputs.number }}", "is_fork": "${{ needs.run-when-command.outputs.is_fork }}"}' > /tmp/pr-meta/meta.json

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pr-meta
          path: /tmp/pr-meta/meta.json
          retention-days: 1
